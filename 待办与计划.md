# 待办与未来计划

## 待修 Bug

1. **习惯休息日逻辑**：`rest_days` 在日历和热力图中应该灰掉或隐藏，目前只在仪表盘过滤了
2. **习惯打卡补签**：目前没打卡不能补签（这是设计意图，不是bug——确认无需修改）

## 待做模块（按计划文件优先级）

### 第二步：核心模块完善
- [x] 日记：月历 + 心情 + 自动记录（OKR完成/习惯打卡）
- [x] 日记与OKR/习惯的联动：任务打勾 → 自动写日记

### 第三步：灵感库升级
- [x] 卡片网格布局
- [x] 图片支持（上传 + 链接）
- [x] 批注/应用标记（状态流转：刚收集→已做笔记→已实践→已废弃）
- [x] Inbox + 快速捕捉
- [x] 标签云、模糊搜索

### 第四步：AI + 复盘 + 回顾
- [x] AI 助手（心情趋势分析、灵感应用建议、跨界连接）
- [x] 月度复盘（数据自动汇总 + 4步引导式复盘）
- [x] 回顾增强（塔罗式随机抽卡：日记/灵感/成就）

### 第五步：联动 + 打磨
- [ ] 模块间联动
- [x] 标签系统升级（标签云、改名、合并、删除、展开查看）
- [ ] 数据导出/导入
- [ ] 今日仪表盘增强

## 技术备忘

### 添加新 Supabase 表字段的方法
直接用 Supabase Dashboard 的 SQL Editor 执行：
```sql
ALTER TABLE 表名 ADD COLUMN IF NOT EXISTS 字段名 类型;
```

### 文件结构
- 所有代码在一个 `幕後.html` 文件里
- `<style>` 标签在 `<body>` 前
- 业务 JS 分组在文件头部（第8行开始）
- 页面初始化和路由在文件尾部（第1545行开始）
- 弹窗容器 `<div id="okr-modal-container">` 统一管理所有弹窗

### JS 代码分段（按行号，截至2026-06-22版本）

| 行号范围 | 内容 |
|----------|------|
| 8-671 | OKR + 习惯 + 今日3件事（核心业务逻辑） |
| 673-1356 | CSS 样式 |
| 1358-1541 | HTML 结构 |
| 1545-尾声 | 页面切换、仪表盘数据、愿景版、设置 |
